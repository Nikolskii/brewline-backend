# deps
node_modules/

# build
dist/
build/

# копия спеки внутрь пакета — артефакт сборки, оригинал в openapi/openapi.yaml
packages/api-types/openapi.yaml

# тарболы npm pack (локальная проверка перед публикацией)
*.tgz

# секреты и локальный конфиг — НИКОГДА в публичный репо
.env
.env.local
*.env

# логи, мусор ОС
*.log
.DS_Store

# IDE
.idea/
.vscode/

# локальные переопределения compose (особенности конкретной машины)
docker-compose.override.yml
